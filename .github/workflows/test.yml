name: Test
on:
  push:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: |
          qemu --help
      - name: Install QEMU
        run: |
          sudo apt-get -y install qemu-system

  macos:
    runs-on: macos-latest
    steps:
      - run: |
          qemu --help
      - name: Install QEMU
        run: |
          brew install qemu
  
  windows:
    runs-on: windows-latest
    steps:
      - run: |
          qemu --help
      - name: Install QEMU
        run: |
          choco install qemu
